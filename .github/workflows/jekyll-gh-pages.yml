# This workflow was auto-generated by GitHub and is not applicable to this React Native project.
name: Deploy Jekyll with GitHub Pages dependencies preinstalled

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Not applicable to this React Native / Expo project."
